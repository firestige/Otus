<?xml version="1.0" encoding="ISO-8859-1" ?>
<!--
  UAC (User Agent Client) default scenario
  Flow: INVITE -> 100 Trying -> 180 Ringing -> 200 OK -> ACK -> [hold] -> BYE -> 200 OK
-->
<scenario name="UAC_default">

  <!-- Send INVITE with SDP offer -->
  <send retrans="500">
    <![CDATA[
      INVITE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: <sip:uac@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: <sip:[service]@[remote_ip]:[remote_port]>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      Contact: <sip:uac@[local_ip]:[local_port]>
      Max-Forwards: 70
      Subject: SIPp VoIP Simulation
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=uac 53655765 2353687637 IN IP[local_ip_type] [local_ip]
      s=SIPp UAC Session
      c=IN IP[media_ip_type] [media_ip]
      t=0 0
      m=audio [media_port] RTP/AVP 0 8 18
      a=rtpmap:0 PCMU/8000
      a=rtpmap:8 PCMA/8000
      a=rtpmap:18 G729/8000
      a=sendrecv
    ]]>
  </send>

  <!-- Accept 100 Trying (optional) -->
  <recv response="100" optional="true">
  </recv>

  <!-- Accept 180 Ringing (optional) -->
  <recv response="180" optional="true">
  </recv>

  <!-- Wait for 200 OK; rtd=true records response time -->
  <recv response="200" rtd="true">
  </recv>

  <!-- Send ACK -->
  <send>
    <![CDATA[
      ACK sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: <sip:uac@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: <sip:[service]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 1 ACK
      Contact: <sip:uac@[local_ip]:[local_port]>
      Max-Forwards: 70
      Content-Length: 0

    ]]>
  </send>

  <!-- Stream G.711 PCMU RTP audio to UAS for the call duration.
       SIPp sends packets from the pcap using the SDP-negotiated
       RTP destination IP:port; UAS echoes them back (-rtp_echo). -->
  <nop>
    <action>
      <exec play_pcap_audio="/scenarios/rtp_g711u.pcap"/>
    </action>
  </nop>

  <!-- Hold the call -->
  <pause milliseconds="$CALL_DURATION_MS"/>

  <!-- Send BYE to terminate -->
  <send retrans="500">
    <![CDATA[
      BYE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: <sip:uac@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: <sip:[service]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 2 BYE
      Contact: <sip:uac@[local_ip]:[local_port]>
      Max-Forwards: 70
      Content-Length: 0

    ]]>
  </send>

  <!-- Wait for 200 OK to BYE -->
  <recv response="200">
  </recv>

  <!-- SIPp statistics labels -->
  <ResponseTimeRepartition value="10, 20, 30, 40, 50, 100, 150, 200"/>
  <CallLengthRepartition value="10, 50, 100, 500, 1000, 5000, 10000"/>

</scenario>
