# Docker build-time environment variables for the capture-agent builder container.
# All non-comment, non-empty lines are automatically passed to 'docker buildx build'
# as --build-arg flags by the Makefile — no Makefile changes needed.
#
# Fill in your internal Nexus addresses before running 'make docker-build'.

# ---------------------------------------------------------------------------
# Go Module Proxy
# Point to your internal Nexus Go proxy repository.
# Use ,direct to fall back to VCS when a module isn't cached in Nexus.
# Omit ,direct in fully air-gapped environments.
# ---------------------------------------------------------------------------
GOPROXY=http://<NEXUS_HOST>/repository/go-proxy,direct

# ---------------------------------------------------------------------------
# Go Checksum Database
# Disable sum.golang.org verification for all modules (*).
# Required when sum.golang.org is unreachable from the internal network.
# You can narrow the scope to specific module path prefixes, e.g.:
#   GONOSUMDB=corp.example.com/*
# ---------------------------------------------------------------------------
GONOSUMDB=*

# ---------------------------------------------------------------------------
# Go Modules mode
# 'on' is the default since Go 1.16 and is kept here for compatibility with
# any tooling that still checks this variable.
# ---------------------------------------------------------------------------
GO111MODULE=on

# ---------------------------------------------------------------------------
# GOPATH — explicit path inside the builder container.
# Not strictly required in module mode but set here for clarity and in case
# any tool relies on it (e.g. go install, some linters).
# ---------------------------------------------------------------------------
GOPATH=/go
