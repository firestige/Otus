# Docker build-time environment variables for the capture-agent builder container.
# All non-comment, non-empty lines are automatically passed to 'docker build'
# as --build-arg flags by the Makefile.

# ---------------------------------------------------------------------------
# DNS servers injected into /etc/resolv.conf inside the build container.
# Required when internal yum mirrors or Go proxy are behind an internal DNS.
# ---------------------------------------------------------------------------
DNS1=<DNS1>
DNS2=<DNS2>

# ---------------------------------------------------------------------------
# Go Module Proxy
# ---------------------------------------------------------------------------
GOPROXY=http://nexus3.msxf.com/repository/go/,direct

# ---------------------------------------------------------------------------
# Go Checksum Database
# Disable sum.golang.org verification (unreachable in internal network).
# ---------------------------------------------------------------------------
GONOSUMDB=*

# ---------------------------------------------------------------------------
# Go Modules mode
# ---------------------------------------------------------------------------
GO111MODULE=on

# ---------------------------------------------------------------------------
# GOPATH inside the builder container
# ---------------------------------------------------------------------------
GOPATH=/go
